[project]
name = "rag-query"
version = "0.1.0"
description = "RAG query service — answering user questions"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.32.0",
    "qdrant-client>=1.12.0",
    "ollama>=0.4.0",
    "pydantic-settings>=2.6.0",
]
